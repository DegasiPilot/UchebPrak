<UserControl x:Class="UchebPrak.Components.StudentUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UchebPrak.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock>Студент</TextBlock>
                <ComboBox DisplayMemberPath="{Binding Student.Familia}" x:Name="StudentCb" Width="100" Margin="10 0 0 0"></ComboBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 5 0 0">
                <TextBlock>Оценка</TextBlock>
                <TextBox Text="{Binding Ocenka}" x:Name="OcenkaTb" Width="20" Margin="10 0 0 0" TextChanged="OcenkaTb_TextChanged"></TextBox>
            </StackPanel>
            <Button Margin="0 5 0 0" x:Name="EnterBtn" Click="EnterBtn_Click">Подтвердить</Button>
        </StackPanel>
    </Grid>
</UserControl>
